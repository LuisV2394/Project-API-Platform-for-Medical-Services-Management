tags:
  - Units
summary: Get a unit by ID
parameters:
  - in: path
    name: unit_id
    required: true
    schema:
      type: integer
responses:
  200:
    description: Unit found
  404:
    description: Unit not found